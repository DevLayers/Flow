import qs.modules.common

HyprlandTabPlaceholder {
    icon: "keyboard"
    title: Translation.tr("Input")
    description: Translation.tr("Keyboard layout and options, key repeat, mouse and touchpad behaviour, the cursor, and per-device overrides will live here.")
}
