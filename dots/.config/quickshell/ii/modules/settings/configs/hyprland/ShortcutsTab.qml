import qs.modules.common

HyprlandTabPlaceholder {
    icon: "keyboard_command_key"
    title: Translation.tr("Shortcuts")
    description: Translation.tr("Every keybind with the file it comes from, an editor that can replace the stock ones, and the default apps they launch will live here.")
}
