import qs.modules.common

HyprlandTabPlaceholder {
    icon: "filter_alt"
    title: Translation.tr("Rules")
    description: Translation.tr("Per-app window rules with a live list of what currently matches, plus raw window, layer and workspace rules, will live here.")
}
