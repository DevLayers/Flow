import qs.modules.common

HyprlandTabPlaceholder {
    icon: "tune"
    title: Translation.tr("All options")
    description: Translation.tr("Every Hyprland option there is, grouped and searchable, each showing the value Hyprland itself reports, will live here.")
}
